version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd front
      - npm ci
  build:
    commands:
      - npm run build

artifacts:
  base-directory: front/out
  files:
    - '**/*'
